<div class="row">
    <div class="col-12">
        <button
            type="{{$type}}"
            name="{{$name}}"
            class="btn {{$size}} {{$buttonBackGround}} {{$dir}}"
        >
            {{$text}}</button>
    </div>
</div>

